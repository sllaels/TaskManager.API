@inherits LayoutComponentBase
@inject NavigationManager navigation
@inject CurrentUserService currentUs
<div class="page">
    <div class="sidebar">
        @if (currentUs.IsAuthenticated)
        {
            <NavMenu />
        }
    </div>

    <main>
        <article class="content px-4">
            @Body
        </article>
    </main>
</div>
